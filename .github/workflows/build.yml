- name: Inject-Smart-Storage
        run: |
          cp smart_storage.c kernel_source/fs/smart_storage.c
          # استدعاء دالة التحسين في نظام الملفات الافتراضي
          sed -i 's/vfs_read/optimize_storage_layout(file); vfs_read/g' kernel_source/fs/read_write.c
